select * from products
where Id = 3002

select * from ProductStocks ps 
where ps.LocationId  in (
select id from locations
where name like '%เชียงใหม่%'or id = 1
)and ps.ProductId = 3002

select l.id,l.name,p.id,p.Name,
p.Quantity ,ps.QtyOnHand ,ps.QtyAvailable,ps.QtyDamaged,ps.QtyReceive  
from ProductStocks ps 
join products p on p.id = ps.ProductId 
join locations l on l.id = ps.locationid
where ps.ProductId = 3002
and l.id in (1,3006,3005,1012)

select *FROM  locations

select * from StockTransactions st 

select * from DamageReturnDocs drd 

select *FROM  orderdt
